"""AI 层：大模型编排、工具调用、知识库检索与规则降级。

设计原则
--------
1. **数字不由模型产生**：所有金额、月数、结余都来自数据库查询与确定性计算，
   模型只负责选择工具与组织语言。这直接对应参考项目 system_prompt 里
   "必须展示计算口径、不要把估算写成精确值"的要求。
2. **写操作两段式**：模型只能提出"预览"，用户确认后才落库，并写入
   ``ai_action_log`` 审计表。
3. **无 Key / 无依赖可用**：没有 API Key 时规则引擎接管记账与可负担性分析，
   不抛异常、不影响其它接口。
"""

from __future__ import annotations

__all__ = ["agent", "knowledge", "llm", "prompts", "tools"]
